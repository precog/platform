# auth-v1 - ReportGrid auth service (v1)
#
#

description "auth-v1 - ReportGrid auth service (v1)"

start on runlevel [2345]
stop on runlevel [06]

respawn

pre-start script
  [ -d /var/run/precog ] || install -m 755 -o precog -g precog -d /var/run/precog
end script

script
  if [ -f /etc/default/auth-v1 ]; then . /etc/default/auth-v1; fi
  su precog -s "/bin/bash" -c "/usr/bin/java $JAVA_OPTS -Dlogback.configurationFile=/etc/precog/auth-v1.logging.xml -jar /usr/share/java/auth-v1.jar --configFile /etc/precog/auth-v1.conf >> /var/log/precog/auth-v1.stdout.log 2>&1"
end script

post-start script
  echo `status auth-v1 | egrep -o '[0-9]+$' | head -n1` > /var/run/precog/auth-v1.pid
end script
