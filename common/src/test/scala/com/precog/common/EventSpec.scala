package com.precog.common

import org.specs2.mutable._

class EventSerializationSpec extends Specification {
  "serialization of an event" should {
    "read back the data that was written" in {
      todo
    }
  }
}


// vim: set ts=4 sw=4 et:
