package com.precog.daze

import org.specs2.mutable._

class MemoizerSpec extends Specification {
  "memoization points" should {
    "be inserted into all memoizable points in the DAG" in todo
  }
}

// vim: set ts=4 sw=4 et:
