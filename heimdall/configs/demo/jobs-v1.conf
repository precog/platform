server {
  port = 40066
  sslPort = 40067
  sslEnable = false

  log {
    level = "info"
    console = true
    filename = "/var/log/precog/jobs-v1-demo.server.log"
    roll = "daily"
    use_parents = false
  }
}

services {
  jobs {
    v1 {
      log {
        level = "debug"
        console = true
        filename = "/var/log/precog/jobs-v1-demo.log"
        roll = "daily"
        use_parents = false
      }

      requestLog {
        enabled = false 
        file = "/var/log/precog/jobs-v1-demo.request.log"
        roll = "daily"
        fields = "date time c-ip cs-method cs-uri-stem cs-uri-query sc-status cs-content"
        formatter = "w3cs"
        excludePaths = ["/jobs/v1/health"]
      }

      zookeeper {
        hosts = "localhost:2181"
        jobId {
          path = "/jobs/demo/v1/jobId"
        }
      }

      mongo {
        servers = ["localhost"]
        database = "jobs_v1_demo"
        jobsCollection = "jobs"
        messagesCollection = "job_messages"
      }

      auth {
        service {
          protocol = "http"
          host = "localhost"
          port = 8080
          path = "/security/v1/"
        }
      }
    }
  }
}
