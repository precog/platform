# beta-ingest-v1 - ReportGrid beta ingest service (v1)
#
#

description "beta-ingest-v1 - ReportGrid beta ingest service (v1)"

start on runlevel [2345]
stop on runlevel [06]

respawn

pre-start script
  [ -d /var/run/precog ] || install -m 755 -o precog -g precog -d /var/run/precog
end script

script
  if [ -f /etc/default/beta-ingest-v1 ]; then . /etc/default/beta-ingest-v1; fi
  su precog -s "/bin/bash" -c "/usr/bin/java $JAVA_OPTS -Dlogback.configurationFile=/etc/precog/beta-ingest-v1.logging.xml -jar /usr/share/java/beta-ingest-v1.jar --configFile /etc/precog/beta-ingest-v1.conf >> /var/log/precog/beta-ingest-v1.stdout.log 2>&1"
end script

post-start script
  echo `status beta-ingest-v1 | egrep -o '[0-9]+$' | head -n1` > /var/run/precog/beta-ingest-v1.pid
end script
