server {
  port = 31060
  sslPort = 31061
  sslEnable = false

  log {
    level = "info"
    console = true
    filename = "/var/log/precog/ingest-v2-b.server.log"
    roll = "daily"
    use_parents = false
  }
}

services {
  ingest {
    v2 {
      log {
        level = "debug"
        console = true
        filename = "/var/log/precog/ingest-v2-b.log"
        roll = "daily"
        use_parents = false
      }

      requestLog {
        enabled = true
        file = "/var/log/precog/ingest-v2-b.request.log"
        roll = "daily"
        fields = "date time c-ip cs-method cs-uri-stem cs-uri-query sc-status cs-content"
        formatter = "w3cs"
        excludePaths = [ "/ingest/v2/health" ]
      }

      ingest {
        tmpdir = "/srv/tmp"
      }

      eventStore {
        systemId = "special_b"
        local {
          topic = "local_event_cache_b"
          broker {
            host = "localhost"
            port = 9082
          }
        }
        central {
          topic = "central_event_store"
          zk {
            connect = "localhost:2181"
          }
        }
      }

      eventService {
        protocol = "http"
        host = "localhost"
        port = 31060
      }

      shard {
        protocol = "http"
        host = "localhost"
        port = 31070
      }

      accounts {
        service {
          protocol = "http"
          host = "localhost"
          port = 31064
          path = "/accounts/v1/"
          user = "operations@precog.com"
          password = "all them rule to one"
        }
      }

      security {
        service {
          protocol = "http"
          host = "localhost"
          port = 31062
          path = "/security/v1/"
        }
        cached = true
        rootKey = "A594581E-E472-4424-AAC1-86CFCCAF8A71"
      }

      jobs {
        service {
          protocol = "http"
          host = "localhost"
          port = 31066
          path = "/jobs/v1/"
        }
      }
    }
  }
}
