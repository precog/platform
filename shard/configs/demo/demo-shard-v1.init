# demo-shard-v1 - ReportGrid demo shard service (v1)
#
#

description "demo-shard-v1 - ReportGrid demo shard service (v1)"

start on runlevel [2345]
stop on runlevel [06]

limit nofile 65535 65535

respawn

pre-start script
  [ -d /var/run/precog ] || install -m 755 -o precog -g precog -d /var/run/precog
end script

script
  if [ -f /etc/default/demo-shard-v1 ]; then . /etc/default/demo-shard-v1; fi
  su precog -s "/bin/bash" -c "/usr/bin/java $JAVA_OPTS -Dlogback.configurationFile=/etc/precog/demo-shard-v1.logging.xml -jar /usr/share/java/demo-shard-v1.jar --configFile /etc/precog/demo-shard-v1.conf >> /var/log/precog/demo-shard-v1.stdout.log 2>&1"
end script

post-start script
  echo `status demo-shard-v1 | egrep -o '[0-9]+$' | head -n1` > /var/run/precog/demo-shard-v1.pid
end script
